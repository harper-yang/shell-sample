#!/bin/bash

# shell中的条件

# -eq 等于
# -gt 大于
# -lt 小于
# -ge 大于或等于
# -le 小于或等于

# [$var -ne 0 -a $var -gt 2] 逻辑与-a

#[ $var -ne 0 -o $var -gt 2 ] 逻辑或 -o

# -f 判断是文件
# -x 判断可执行
# -d 判断是目录
# -e 判断文件存在

# 字符串比较 [[ $a=$b ]]

